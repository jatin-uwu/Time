{
  "requires": {
    "db": {
      "kind": "sqlite",
      "credentials": {
        "database": "db.sqlite"
      }
    }
  },
  "build": {
    "tasks": [
      { "for": "hana", "src": "db" },
      { "for": "nodejs", "src": "srv" }
    ]
  }
}